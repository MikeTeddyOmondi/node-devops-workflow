version: "3.6"
services:
    nginx:
        ports:
            - "80:80"
    web_app:
        build:
            context: .
            args:
                NODE_ENV: production
        environment:
            - NODE_ENV=production
        command: node index.js